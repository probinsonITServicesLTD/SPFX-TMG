declare interface ITileMenuGeneratorWebPartStrings {
  PropertyPaneDescription: string;
  BasicGroupName: string;
  DescriptionFieldLabel: string;   
}

declare module 'TileMenuGeneratorWebPartStrings' {
  const strings: ITileMenuGeneratorWebPartStrings;
  export = strings;
}

interface Global {
  Paul: string;
 }
